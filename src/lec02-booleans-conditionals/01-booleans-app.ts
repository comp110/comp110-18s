import { print } from "introcs";

export let main = async () => {

    print("Boolean operator examples");

    // Using the following variables...
    let x: number = 0;
    let y: number = 10;
    let z: number = 10;

    let unc: string = "UNC";
    let duke: string = "Duke";

    // Use the following inequality operators to form comparisons
    // that evaluate to, and therefore print, true.

    // TODO: Write a print statement using each of these operators
    // beneath each comment.

    // Less than: <
    
    // Less than or equal to: <
    
    // Greater than: >
    
    // Greater than or equal to: >=
    
    // Equal to: ===
    
    // Not equal to: !==

};

main();