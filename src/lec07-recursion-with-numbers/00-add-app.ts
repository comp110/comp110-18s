import { print } from "introcs";

export let main = async () => {
    print(add(3, 0));
};

// Challenge: Implement the add function recursively
// using only:
//   + 1 and 
//   - 1
let add = (n: number, m: number): number => {
    return 0;
};

main();