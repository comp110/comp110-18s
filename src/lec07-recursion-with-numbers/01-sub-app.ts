import { print } from "introcs";

export let main = async () => {
    print(sub(3, 0));
};

// Challenge: Implement the sub function recursively
// using only:
//   + 1 and 
//   - 1
let sub = (n: number, m: number): number => {
    return 0;
};

main();