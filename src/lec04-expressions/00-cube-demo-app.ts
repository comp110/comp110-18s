import { print, promptNumber } from "introcs";

export let main = async () => {

    let answer: number;
    
    // TODO: Write other expressions to calculate cube volume.
    answer = 3 * 3 * 3;
    print(answer);

};

main();