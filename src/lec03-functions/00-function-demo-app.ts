import { print, promptNumber } from "introcs";

// TODO: Define the function f

export let main = async () => {

    let input: number = await promptNumber("Enter a number");
    
    // TODO: Call the function f
    let answer: number = 0;

    print(answer);

};

main();